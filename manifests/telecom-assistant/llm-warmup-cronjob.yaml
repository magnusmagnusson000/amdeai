# Keeps DiffusionGemma AIM hot between page loads (vLLM first-token latency).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: llm-warmup
  labels:
    app: llm-warmup
spec:
  schedule: "*/2 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      activeDeadlineSeconds: 180
      template:
        metadata:
          labels:
            app: llm-warmup
        spec:
          restartPolicy: OnFailure
          containers:
            - name: warmup
              image: curlimages/curl:8.18.0
              env:
                - name: LLM_URL
                  value: "http://diffusiongemma-llm.default.svc.cluster.local/v1/chat/completions"
                - name: LLM_MODEL
                  value: "google/diffusiongemma-26B-A4B-it"
                - name: LLM_ENABLE_THINKING
                  value: "true"
              command:
                - sh
                - -c
                - |
                  set -e
                  curl -sf --max-time 120 -X POST "${LLM_URL}" \
                    -H "Content-Type: application/json" \
                    -H "Authorization: Bearer no-key-required" \
                    -d "{\"model\":\"${LLM_MODEL}\",\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}],\"max_tokens\":1,\"chat_template_kwargs\":{\"enable_thinking\":${LLM_ENABLE_THINKING:-true}}}"
